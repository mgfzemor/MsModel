<%= form_with(model: table_type, local: true) do |form| %>
  <% if table_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(table_type.errors.count, "error") %> prohibited this table_type from being saved:</h2>

      <ul>
      <% table_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :prefix %>
    <%= form.text_field :prefix %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
