json.extract! column, :id, :system_name, :database_name, :nn, :uq, :bin, :un, :zf, :g, :active_id, :active_created, :active_updated, :msColumnType_id, :table_id, :created_at, :updated_at
json.url column_url(column, format: :json)
